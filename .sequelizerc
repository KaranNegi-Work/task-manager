const path = require('path');

module.exports = {
  'config': path.resolve('./src/config', 'config.js'), // Update with your custom config path
  'models-path': path.resolve('./src/models', 'models'),
  'migrations-path': path.resolve('./db', 'migrations')
};